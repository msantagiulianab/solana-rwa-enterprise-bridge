<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/>
    </parent>

    <groupId>com.solana.rwa</groupId>
    <artifactId>solana-rwa-enterprise-bridge</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <name>solana-rwa-enterprise-bridge</name>
    <description>Enterprise bridge between Spring Boot off-chain infrastructure and the Solana blockchain</description>

    <properties>
        <java.version>21</java.version>
        <!--
            Override the Lombok version managed by Spring Boot 3.5.x.
            JDK 23+ silently breaks annotation processing unless a recent Lombok
            is used; 1.18.42 is the minimum that supports JDK 25 (the host JDK).
        -->
        <lombok.version>1.18.42</lombok.version>
        <!--
            Mockito's inline mock maker (used by @MockitoBean / @WebMvcTest) relies
            on Byte Buddy. The versions managed by Spring Boot 3.5.x (Byte Buddy
            1.14.19, Mockito 5.11.0) cannot instrument classes on Java 25, so we
            upgrade both to versions that support the host JDK.
        -->
        <byte-buddy.version>1.17.8</byte-buddy.version>
        <mockito.version>5.20.0</mockito.version>
    </properties>

    <dependencies>
        <!-- Spring Web: REST controllers & HTTP client for Solana JSON-RPC -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Spring Boot Actuator: health checks & monitoring endpoints -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- Spring Data JPA: PostgreSQL persistence for compliance & audit tables -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- Bean Validation: request DTO validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- PostgreSQL Driver -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- H2: embedded PostgreSQL-compatible database for @DataJpaTest integration tests -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Lombok: boilerplate reduction for DTOs and entities -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Unit / integration test support (JUnit 5, Mockito, AssertJ) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!--
                Explicitly enable full annotation processing. JDK 23+ disables
                annotation processing by default, which silently breaks Lombok
                (no builders/getters/setters generated). Declaring Lombok on the
                annotation processor path makes processor discovery deterministic.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <release>${java.version}</release>
                    <proc>full</proc>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
            <!--
                Run *IT integration tests on every build (H2 / mocked RPC).
                JPA repository tests follow the *IT.java naming convention per
                .clinerules, so they must be included in surefire's default run.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <includes>
                        <include>**/*Test.java</include>
                        <include>**/*IT.java</include>
                    </includes>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
